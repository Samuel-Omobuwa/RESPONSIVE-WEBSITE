// 1.Number: Represents both integer and floating-point numbers.
let age = 25;
let price = 10.89;

// 2. String: Represents a sequence of characters enclosed in single ('')
let name = "Aselemi Divine";
let message = "Hello world"

let fame = ""; null

//3. Boolean: Represent a True or False values.
let islogged = true;
let isApproved = false;

// 4. Undefined: Represents a variable that has been delcalred but not assigned
let username;
console.log(username);  Output: undefined

// 6. Object: Represent a collection of key-value enclosed in curly braces
let person = {
    name: "Alice",
    age: 39;
    IsAdmine: false
};

// 7. Array: Represents a list of values enclosed in square brackets []
let colors = ["red", "green", "blue"];

// 8. Symbol: Represents a unique and imutable value often used as objects
const id = Symbol("id");

// 9. Function: Represens a block of code that can be executed when called.
function greet(name){
    return "Hello", "+ name +"
}